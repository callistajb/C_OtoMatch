<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base Light Theme -->
    <style name="Base.Theme.C_OtoMatch" parent="Theme.Material3.Light.NoActionBar">
        <!-- Brand Colors -->
        <item name="colorPrimary">#091c2a</item> <!-- Navy Blue utama -->
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorPrimaryContainer">#0e2d42</item>
        <item name="colorSecondary">#134b66</item>
        <item name="colorOnSecondary">@android:color/white</item>

        <!-- Background & Surface -->
        <item name="android:colorBackground">@android:color/white</item>
        <item name="colorSurface">@android:color/white</item>
        <item name="colorOnSurface">#091c2a</item>

        <!-- Font Family Global -->
        <item name="android:fontFamily">@font/lato_regular</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">#091c2a</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Toolbar / ActionBar -->
        <item name="android:navigationBarColor">#091c2a</item>
    </style>

    <style name="Theme.C_OtoMatch" parent="Base.Theme.C_OtoMatch" />
</resources>
